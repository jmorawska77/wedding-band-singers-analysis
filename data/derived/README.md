# Derived participant-level data

`R/00_prepare_data.R` writes `data_analysis_final.rds` here. The file is needed
by the analysis scripts but is excluded from version control because it remains
participant-level data.

