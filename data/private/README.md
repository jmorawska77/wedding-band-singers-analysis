# Private input data

Place the source workbook here as:

`wedding_band_singers_data.xlsx`

This directory is excluded from version control because the workbook contains
participant-level data. Do not commit the workbook or any replacement file that
contains individual records.

